{
  "$schema": "https://opencode.ai/config.json",
  "_comment": "Add this block to opencode.json (project-local) or ~/.config/opencode/opencode.json (global). Restart opencode after editing. Replace the placeholder paths with your actual values.",
  "mcp": {
    "kuckuck": {
      "type": "local",
      "command": ["kuckuck-mcp"],
      "environment": {
        "KUCKUCK_KEY_FILE": "/absolute/path/to/.kuckuck-key",
        "KUCKUCK_MCP_ALLOWED_ROOTS": "/absolute/path/to/your/workspace"
      }
    }
  }
}
